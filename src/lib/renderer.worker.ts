// renderer.worker.ts — archived worker stub.
// The original OffscreenCanvas worker implementation was removed from the active codepath
// in favor of main-thread Babylon rendering and an offscreen projection copy. This file
// remains as a harmless placeholder to avoid build issues if referenced. Remove if you
// want to fully delete the worker implementation.

export default null as any;
